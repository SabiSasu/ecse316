We used python 3.8.1.
To run the program, use the same command from the instructions:
python fft.py -m <mode> -i <image>

We used multiple libraries, use the following commands
if they are not installed yet:
pip3 install plotly
pip3 install matplotlib
pip3 install numpy
pip3 install opencv-python